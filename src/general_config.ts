export const generalConfig = {
  /**
   * Poll intervals set for fetching data. Defaults to 5 minutes
   */
  pollInterval: {
    default: 300000, // default intervals
    minute: 60000, // every minute
  },
};
