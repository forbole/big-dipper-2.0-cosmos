query Account($address: String) {
  account(where: {address: {_eq: $address}}) {
    address
    delegationRewards: delegation_rewards(limit: 1, order_by: {block: {height: desc}}) {
      withdrawAddress: withdraw_address
    }
  }
}
