query Proposals ($limit: Int = 7, $offset: Int = 0) {
  proposals: proposal (limit: $limit, offset: $offset, order_by: {proposal_id: desc}) {
    title
    proposalId: proposal_id
    status
    description
  }
  total: proposal_aggregate {
    aggregate {
      count
    }
  }
}

