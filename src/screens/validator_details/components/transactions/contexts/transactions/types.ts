import { UseContextState } from '@src/components/transactions_list/types';

// hasNextPage: boolean;
// isNextPageLoading: boolean;
// items: any[];
// loadNextPage?: (any) => void;
// itemCount?: number;
// loadMoreItems?: (any) => void;
// isItemLoaded?: (index: number) => boolean;

export type TransactionsState = UseContextState;
