{
  "name": "big-dipper-2.0-cosmos",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"turbo run watch\" \"turbo run dev --filter ${0:-web}... --no-cache --concurrency=1\"",
    "build": "turbo run build --filter ${0:-web}...",
    "build:ci": "turbo run build --concurrency=30%",
    "clean": "[ -d \"node_modules\" ] && turbo run clean && rm -rf node_modules",
    "start": "turbo run start --filter=${0:-web}...",
    "format": "prettier --ignore-path ./.gitignore --write \"**/*.{js,ts,tsx,mjs,cjs,md}\"",
    "lint": "turbo run lint --filter ${0:-web}...",
    "lint:ci": "turbo run lint --concurrency=30%",
    "test": "turbo run test --filter ${0:-web}...",
    "test:update:snapshot": "turbo run test --filter ${0:-web}... -- -u",
    "test:ci": "turbo run test --concurrency=30%",
    "e2e": "turbo run e2e --filter ${0:-web}...",
    "e2e:ci": "turbo run e2e --concurrency=30%",
    "graphql:codegen": "turbo run graphql:codegen --continue --concurrency=2",
    "postinstall": "husky install"
  },
  "dependencies": {
    "@changesets/cli": "^2.25.0",
    "dotenv-cli": "^6.0.0"
  },
  "devDependencies": {
    "husky": "^8.0.0",
    "lint-staged": "^13.0.3",
    "prettier": "2.7.1",
    "turbo": "latest"
  },
  "packageManager": "yarn@3.2.4",
  "engines": {
    "node": ">=16.13.0",
    "yarn": ">=3.2.4"
  }
}
