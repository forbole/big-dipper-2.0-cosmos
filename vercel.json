{
  "installCommand": "yarn install",
  "buildCommand": "yarn workspace web next build && mv -f node_modules apps/web/",
  "devCommand": "yarn workspaces web next dev",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "ignoreCommand": "echo 'always build' && exit 1"
}