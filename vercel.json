{
  "installCommand": "yarn install",
  "buildCommand": "yarn next build && cp -rf ../../node_modules ./",
  "devCommand": "yarn next dev",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "ignoreCommand": "echo 'always build' && exit 1"
}